mutation UpdateSkill(
    $id: String!
    $name: String!
) {
    updateSkill (
        id: $id
        name: $name
    ) {
        id
        name
        candidate {
            name
        }
    }
}