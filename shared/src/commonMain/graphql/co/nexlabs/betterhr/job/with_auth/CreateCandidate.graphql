mutation createCandidate(
    $name: String!,
    $email: String!,
    $phone: String!,
    $country_id: String!
) {
    createCandidate (
        name: $name,
        email: $email
        phone: $phone
        country_id: $country_id
    ) {
        id
        name
        email
    }
}