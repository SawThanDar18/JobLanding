query FetchNotifications($status: [String], $search: String, $page: Int!, $limit: Int!) {
    notifications(status: $status, search: $search, page: $page, limit: $limit) {
        id
        candidate_id
        reference_id
        job_id
        title
        status
        noti_type
        updated_at
    }
}