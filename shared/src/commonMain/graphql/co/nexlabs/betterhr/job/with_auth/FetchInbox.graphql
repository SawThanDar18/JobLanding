query FetchNotifications($status: [String], $search: String, $limit: Int!) {
    notifications(status: $status, search: $search, limit: $limit) {
        id
        candidate_id
        reference_id
        job_id
        title
        status
        noti_type
        updated_at
    }
}